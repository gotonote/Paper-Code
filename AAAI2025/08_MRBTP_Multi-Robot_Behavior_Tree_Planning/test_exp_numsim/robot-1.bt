fallback
    condition 1 & 2
    fallback
        sequence
            condition IsSelfTask(2) id=0 act=CMP_A2_D0
            action CMP_A2_D0()
            action FinishTask()
        sequence
            condition 1
            action SelfAcceptTask(2) id=0 act=CMP_A2_D0
